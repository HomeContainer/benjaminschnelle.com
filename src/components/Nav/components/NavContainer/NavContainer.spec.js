/*
import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import NavContainer from './NavContainer';
import classes from './NavContainer.scss';

const child = <div>I am a child</div>;

describe('NavContainer', () => {
  it('renders a div with its children', () => {
    const wrapper = shallow(<NavContainer>{child}</NavContainer>);
    expect(wrapper.type()).to.equal('div');
    expect(wrapper.containsAllMatchingElements([<div>{child}</div>])).to.be.true;
  });

  it('has a .navContainer class', () => {
    const wrapper = shallow(<NavContainer>{child}</NavContainer>);
    expect(wrapper.is(`.${classes.navContainer}`)).to.be.true;
  });
});
*/
